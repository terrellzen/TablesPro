# Theming

The web app will keep semantic theme tokens in `frontend/apps/web/src/styles/theme.css`.

Required tokens include color, radius, shadow, font, and grid spacing variables. Components must reference semantic variables rather than hardcoded application colors.

Light and dark themes use the same token names. Rebranding should usually require editing this one theme file plus replacing the neutral logo asset.
