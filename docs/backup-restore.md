# Backup and Restore

Backups should include the PostgreSQL database and any configured import/export artifact storage.

The default self-hosted path will document `pg_dump`, `pg_restore`, migration ordering, and a restore verification checklist.
